{
  "name": "@robelest/embedded-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp test",
    "test:once": "vp test --run",
    "test:fx": "vp test --run --project fx",
    "test:embedded": "vp test --run --project embedded",
    "test:resolve": "vp test --run --project resolve",
    "test:integration": "vp test --run --project integration"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@robelest/convex-embedded": "workspace:*",
    "@types/node": "^25.5.0",
    "convex": "catalog:",
    "convex-test": "^0.0.36",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "yjs": "^13.6.0"
  }
}
