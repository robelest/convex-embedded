{
  "name": "@robelest/embedded-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp test",
    "test:once": "vp test --run",
    "test:coverage": "vp test --run --coverage",
    "test:fx": "vp test --run fx/**/*.test.ts",
    "test:embedded": "vp test --run embedded/**/*.test.ts",
    "test:resolve": "vp test --run resolve/**/*.test.ts",
    "test:integration": "vp test --run integration/**/*.test.ts"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@robelest/convex-embedded": "workspace:*",
    "@types/node": "^25.5.0",
    "convex": "catalog:",
    "convex-test": "^0.0.36",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "yjs": "^13.6.0"
  }
}
