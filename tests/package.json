{
  "name": "tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp test",
    "test:once": "vp test --run",
    "test:coverage": "vp test --run --coverage",
    "test:consumer": "vp run --filter @robelest/convex-embedded build && vp test --run tests/package/consumer.test.ts",
    "bench": "vp test bench tests/benchmarks",
    "test:embedded": "vp test --run embedded",
    "test:resolve": "vp test --run resolve",
    "test:integration": "vp test --run integration"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@robelest/convex-embedded": "workspace:*",
    "@robelest/fx": "0.0.1-preview.4",
    "@types/node": "^25.5.0",
    "convex": "catalog:",
    "convex-test": "^0.0.36",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "yjs": "^13.6.0"
  }
}
