{
  "name": "convex-resolve-monorepo",
  "private": true,
  "workspaces": {
    "packages": ["packages/*", "demos/*"],
    "catalog": {
      "convex": "^1.17.0",
      "vitest": "^4.0.18",
      "typescript": "^5.9.0"
    }
  },
  "scripts": {
    "test": "bun --filter '*' test:once",
    "build": "bun --filter convex-resolve build",
    "dev:convex": "bunx convex dev",
    "dev:tanstack": "bun run --filter demo-tanstack-start dev"
  },
  "dependencies": {
    "convex": "^1.32.0",
    "convex-resolve": "workspace:*"
  },
  "devDependencies": {
    "@convex-dev/action-retrier": "^0.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20260308.1"
  }
}
