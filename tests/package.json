{
  "name": "tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp test",
    "test:once": "vp test --run",
    "test:coverage": "vp test --run --coverage",
    "test:consumer": "vp run --filter @robelest/convex-embedded build && vp test --run resolve/package-consumer.test.ts",
    "bench": "vp test bench tests/benchmarks",
    "test:fx": "vp test --run fx",
    "test:embedded": "vp test --run embedded",
    "test:resolve": "vp test --run resolve",
    "test:integration": "vp test --run integration"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@robelest/convex-embedded": "workspace:*",
    "@types/node": "^25.5.0",
    "convex": "catalog:",
    "convex-test": "^0.0.36",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "yjs": "^13.6.0"
  }
}
