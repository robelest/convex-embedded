{
  "name": "@robelest/convex-edge-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:once": "vitest run",
    "test:embedded": "vitest run --project embedded",
    "test:resolve": "vitest run --project resolve",
    "test:integration": "vitest run --project integration"
  },
  "devDependencies": {
    "@robelest/convex-embedded": "workspace:*",
    "@robelest/convex-resolve": "workspace:*",
    "convex": "catalog:",
    "convex-test": "^0.0.36",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "yjs": "^13.6.0"
  }
}
